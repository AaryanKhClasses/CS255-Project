{
    "name": "expense-tracker",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run start --prefix backend\""
    }
}
